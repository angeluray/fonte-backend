<%= form_with(model: market_category) do |form| %>
  <% if market_category.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(market_category.errors.count, "error") %> prohibited this market_category from being saved:</h2>

      <ul>
        <% market_category.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
